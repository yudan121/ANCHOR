# Version policy

ANCHOR v1 is the thesis-compatible release used for the primary reported experiments. ANCHOR v2 keeps the same public API and training pipeline, and updates the rho policy. Use Git tags to switch between release states.
